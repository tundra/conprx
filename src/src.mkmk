# Copyright 2014 the Neutrino authors (see AUTHORS).
# Licensed under the Apache License, Version 2.0 (see LICENSE).

include('utils', 'src_utils.mkmk')
include('agent', 'src_agent.mkmk')

objects = get_group("objects")
objects.add_dependency(get_external("src", "agent", "objects"))
objects.add_dependency(get_external("src", "utils", "objects"))

all = add_alias("all")
all.add_member(get_external("src", "agent", "all"))
all.add_member(get_external("src", "utils", "all"))
